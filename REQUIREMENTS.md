# 実況ブラウザ LiveFake - 要件定義書

## 概要

Tauri v2 (Rust バックエンド + React/TypeScript フロントエンド) ベースの日本語掲示板専用実況ブラウザ。
**対応 OS: Windows 10 / Windows 11 (64ビット)。**
対応サイト: 5ch.io / したらば / JPNKN。
Portable 形式 (実行フォルダ配下 `data/` で自己完結) で配布する。

本書は実装済み機能を要件として記述したものである (v0.0.98 時点)。

---

## 1. UI・レイアウト

### 1.1 メイン画面構成

- メニューバー (ファイル / 編集 / 表示 / 板 / スレッド / 設定 / ヘルプ)
- ツールバー (板一覧トグル・板更新・アドレスバー・自動更新トグル・NG ボタン)
- 板ボタンバー (設定で ON 時、お気に入り板をボタン表示・ドラッグ並べ替え)
- 3ペイン: 板一覧 / スレ一覧 / レス表示 (スレ一覧とレス表示は同一領域を切替表示)
- 新着レスペイン (レスペイン右側、開閉式)
- 書き込みウィンドウ (下部、開閉式)
- ステータスバー (状態メッセージ・スレ数・未読数)

### 1.2 リサイザー

- 全ペイン境界・新着ペイン高さ・書き込みウィンドウ高さ (120〜800px) をドラッグでリサイズできること
- リサイズ結果は永続化し次回起動時に復元すること
- 表示メニューから「レイアウトリセット」ができること

### 1.3 タブ操作

- 板タブ (上段)・スレタブ (下段) の2段構成
- ドラッグで並べ替え、中クリックで閉じる (スレタブ)、右クリックメニュー
- スレタブごとにレス内容・選択レス・スクロール位置・新着位置をキャッシュし、切替時に復元すること
  - 最下部で離れたタブへ戻った場合は最下部 (最新レス) を表示すること
- 最大タブ数は設定可能 (1〜50、既定20)。上限到達時は新規タブを開かない
- セッション復元: 起動時に前回のタブ構成・アクティブタブを復元 (設定で ON/OFF)

### 1.4 テキスト表示設定

- フォント (システムフォントピッカー)・太字
- 文字サイズ: 板 / スレ / レス各ペイン (8〜20px)、レスヘッダ (8〜20)、新着レス (8〜24)、ポップアップ (8〜40)、書き込み (10〜24)
- レス ID・新着 ID の文字サイズ補正とフォント個別指定
- レス間隔 (0〜40px)
- ライト / ダークテーマ (OS ウィンドウテーマ連動)

### 1.5 キーボードショートカット

- ↑↓ / PageUp / PageDown / Home / End によるレスエリアスクロール (入力欄フォーカス時を除く)
- Shift+Enter または Ctrl+Enter で書き込み送信 (設定で選択)
- Escape で各種ポップアップ・メニュー・ダイアログを閉じる

### 1.6 ユーザー CSS カスタマイズ

- `data/custom.css` を起動時に読み込み、組み込みスタイルの後に適用すること
- ファイルが無い場合は説明コメント付きテンプレートを自動生成すること
- メニューから再読み込みでき、再起動なしで反映されること
- 詳細仕様は `docs/CSS_CUSTOMIZE.md` に定める

---

## 2. BBS 読み込み・表示機能

### 2.1 対応サイト

#### 2.1.1 5ch.io 系

- 判別: ホストが `.5ch.io` / `.5ch.net` / `.2ch.net`
- `*.5ch.net` は `*.5ch.io` へ正規化して扱う
- 一覧: subject.txt、レス: dat (取得不可時は read.cgi HTML をフォールバック解析)
- エンコーディング: Shift_JIS

#### 2.1.2 したらば (jbbs.shitaraba.net)

- 一覧: subject.txt、レス: rawmode.cgi (DT/DD 形式 HTML パーサも保持)
- エンコーディング: EUC-JP

#### 2.1.3 JPNKN (bbs.jpnkn.com)

- 一覧: subject.txt、レス: dat (5ch と同形式)
- エンコーディング: Shift_JIS

### 2.2 エンコーディング自動判定

UTF-8 / Shift_JIS / EUC-JP のスコアリングによる自動判定を行うこと (`detect_encoding`)。

### 2.3 板一覧

- 5ch BBS Menu を取得しカテゴリツリー表示。展開状態・スクロール位置を永続化
- 板名のインクリメンタル検索 (検索中は自動展開)
- お気に入り板 (★トグル・専用カテゴリ・ドラッグ並べ替え)

### 2.4 外部板登録

- したらば・JPNKN の板を URL + 板名で登録でき、「外部板」カテゴリに表示・削除できること

### 2.5 スレッド一覧

- 列: 取得済み / 番号 / タイトル / レス / 既読 / 新着 / 最終取得 / Since / 勢い (バー表示)
- 全列ソート (昇順/降順)、列幅リサイズ・リセット・永続化、更新時ソート維持 (設定)
- タイトル検索 (履歴最大20件)、既読/未読管理、カスタムタイトル
- dat 落ちキャッシュ一覧表示、お気に入りスレのみ表示 (新着数一括確認)
- スレタイ NG ワード管理

### 2.6 レス表示

- レスヘッダ: レス番 / [自分]・[自分宛] 自動検出 / 名前 / メール (sage 強調) / ワッチョイ / 被参照数▼ / New! / 日付 / ID (色分け・出現数 n/総数、2回以上=青・5回以上=赤) / BE 番号
- アンカー: `>>N`・`>N`・範囲 `>>2-10`・複数 `>>2,3` をリンク化し、ホバーで多段ポップアップ
- 同一 ID レス一覧ポップアップ、被参照ポップアップ
- URL 自動リンク (ttp:// 等の省略形補完)、sssp:// BE アイコン表示
- AA 自動判定で等幅表示 (右クリックで手動切替)
- 「ここから新着」セパレータ、栞・スクロール位置の自動保存、dat 落ち検出表示
- ナビバー: 着数 (NG 件数)・サイズ / リンクフィルタ (画像・動画・外部リンク) / Top・New・End・レス番ジャンプ

### 2.7 自動更新

- 間隔 10〜300 秒 (既定15秒)、ツールバーで ON/OFF
- アクティブタブは全件取得・UI 更新、バックグラウンドタブは差分取得 (sinceResNo)
- スレ一覧もサイレント更新

### 2.8 自動スクロール

- 新着取得時に最下部へ自動スクロール (設定で ON/OFF)
- ユーザーのホイール・クリック等の操作で即中断すること
- スムーススクロール設定 (OFF 時は WebView2 起動引数で無効化、再起動後反映)

---

## 3. 書き込み (投稿) 機能

### 3.1 フォーム

- 名前 (履歴 datalist・最大20件) / メール / 本文 / sage チェック (ON 時メール欄 sage 固定)
- 文字数・行数カウンタ、フォントサイズ設定
- レス / 新スレモード切替 (新スレ時はスレタイ入力欄)
- 送信ショートカット: Shift+Enter / Ctrl+Enter (設定選択)

### 3.2 サイト別投稿

- 5ch: bbs.cgi。確認画面・UPLIFT 同意フォームの hidden fields を自動処理して再送信
- したらば: write.cgi (EUC-JP)。書き込み・スレ立て
- JPNKN: bbs.cgi。書き込み・スレ立て
- 投稿レスポンス HTML から成功 / エラー / 確認画面を判定すること

### 3.3 投稿後処理

- 成功時: 本文クリア・名前履歴保存・再取得・自分の投稿を本文照合で検出し [自分] ラベル付与
- 書き込み履歴を最大50件表示できること (起動中のみ)

### 3.4 引用

- レスのダブルクリックで `>>N` 挿入、右クリックから「ここにレス」「名前付き引用」

### 3.5 スレッド立て

- 専用ダイアログ (リサイズ可・サイズ永続化)。成功時に新スレをタブで開くこと

---

## 4. Cookie

- BE / UPLIFT / どんぐり等のログイン認証機能は搭載しない (セキュリティ方針により削除)
- 投稿時の MonaTicket 等は Cookie jar でセッション中のみ維持し、ファイルへ永続化しないこと
- Cookie 値をログに記録しないこと

---

## 5. 画像機能

### 5.1 表示

- 画像 URL (jpg/jpeg/png/gif/webp) の自動サムネイル (50〜600px 設定)
- 画像サイズ制限 (KB、HEAD の Content-Length 判定、超過は「クリックで表示」)
- 画像プレビュー表示の ON/OFF
- クリックで別ウィンドウ表示 (data URL 化)
- ホバープレビュー (遅延 0〜2000ms / Ctrl+ホバー即時 / Ctrl+ホイールズーム 10〜500%)

### 5.2 画像 URL 置換

- `data/ImageViewURLReplace.txt` (TSV: pattern⇥replacement[⇥referer]) による URL 変換
- 設定画面にルール数表示・デフォルトリセット・データフォルダを開くボタン

### 5.3 保存

- 右クリックから設定フォルダへ保存 (初回はフォルダ選択)

---

## 6. 字幕機能

- 半透明・最前面の別ウィンドウに新着レスを表示すること (ドラッグ移動可)
- 表示要素: スレタイ・レス番・名前・ID (ハイライト色反映)・日付・本文 (画像除外・ワードハイライト適用)
- 設定: 本文フォント (10〜96) / メタ (8〜48) / 透明度 (0.1〜1.0) / 最前面 / ID サイズ補正・フォント / 位置リセット
- NG (非表示) レスは字幕に出さないこと

---

## 7. NG フィルター機能

- タイプ: ワード / ID / 名前 / スレタイ
- `/pattern/` 形式の正規表現対応 (大文字小文字無視)
- モード: 非表示 / 画像のみ非表示
- スコープ: 全体 / この板 / このスレ
- 登録経路: NG パネル / 設定パネル / レス右クリック / スレ一覧右クリック / BE・ワッチョイメニュー
- 非表示レスは新着ペイン・字幕・読み上げから除外すること

---

## 8. ハイライト機能

- 15色パレットでワード (本文)・名前・ID を色分けできること
- ID ハイライトは当日限り有効 (日付変更で自動リセット)
- レス右クリックの色グリッドから即適用、設定パネルで一覧管理
- 字幕ウィンドウへ同期すること

---

## 9. 音声読み上げ (TTS)

- エンジン: OFF / SAPI / 棒読みちゃん / VOICEVOX
- SAPI: COM を STA スレッドで実行。ボイス選択・速度 (-10〜+10)・音量 (0〜100)
- 棒読みちゃん: RemoteTalk.exe パス設定。ヌルバイト除去・2000文字制限のインジェクション対策必須
- VOICEVOX: エンドポイント設定 (既定 127.0.0.1:50021)・話者・速度 (0.5〜2.0)・ピッチ (-0.15〜+0.15)・抑揚 (0〜2.0)・音量 (0〜2.0)
- 共通: 自動読み上げ ON/OFF、最大文字数 (0=無制限、超過は「長文のため以下省略」)、逐次キュー、テスト/停止
- 前処理: HTML タグ・エンティティ除去、URL 除去 (YouTube→「ユーチューブ」)
- レス番プレフィックスはサイト別 (レス / したらば / ジャパンくん N番さん)
- 読み上げ辞書: キーワード置換・「全置換」フラグ (`tts-dict.json`)
- 右クリック「このレスから読み上げ」対応

---

## 10. 新着レスペイン

- 自動更新で取得した新着レス (バックグラウンドタブ含む) をキューに積み、1件ずつ表示すること
- 表示5秒 → 次へ。溢れる本文は2秒静止後ゆっくり自動スクロール
- 表示要素: スレタイ・レス番・名前・ID・時刻・本文 (200文字まで)。クリックでジャンプ
- NG (非表示) レスは表示しない

---

## 11. 検索機能

- 板検索・お気に入りスレ検索 (インクリメンタル)
- スレ一覧検索 (部分一致・ハイライト・履歴)
- レス検索2モード: レス抽出 (合致レスのみ表示) / スレ内検索 (n/m カウンタ・次ヒットジャンプ)
- 検索履歴はスレ / レス別に最大20件、再利用・個別削除可能

---

## 12. 設定ファイル (Portable 形式)

- 保存先: 実行フォルダ配下 `data/` (環境変数 `EMBER_DATA_DIR` で上書き可)
- `settings.ini`: [App] / [Speech] / [Posting] / [Proxy] セクション
- JSON: ng-settings / id-highlights / text-highlights / cookies / board-catalog / external-boards / thread-history / read_status / bookmarks / scroll-positions / session_tabs / session_board_tabs / layout_prefs / window_size / tts-dict / name-history / my-posts / search-history ほか
- SQLite: `cache.db` (thread_cache テーブル、スレのレス JSON を丸ごとキャッシュ)
- `custom.css` / `ImageViewURLReplace.txt` / `eventlog/` (日別ログ・保持日数で自動削除)
- `data/` のコピーで環境移行できること

---

## 13. 設定 UI

- カテゴリ: 表示 / 書き込み / 読み上げ / 読み上げ辞書 / 字幕 / プロキシ / NG / ハイライト / 情報
- 各項目は変更即時反映し、対応する永続化先 (INI/JSON) に自動保存すること

---

## 14. アップデートチェック

- `latest.json` を取得し semver 比較で更新有無を判定すること
- バージョン情報ダイアログで自動チェックし、更新時はダウンロードページを開けること

---

## 15. セキュリティ・制約

- 通信先を対応ドメインに限定すること (`is_allowed_url`)
- レス本文 HTML はサニタイズしてから描画すること。`javascript:` 等の危険スキームをブロック
- Cookie 値 (Be3M / Be3D / sid) をログに記録しないこと
- 二重起動を防止すること
- **GPL ライセンス (GPLv2 / GPLv3 / AGPL) のライブラリは使用禁止**

---

## 16. 既知の制限 (対応予定)

- **プロキシ (HTTP / SOCKS5 / SOCKS4)**: 設定 UI・保存・クライアント生成は実装済みだが、HTTP クライアントに反映されないため動作しない。プロキシパスワードは平文保存
